## Допущения в архитектуре

1) Не реализована полноценная прослойка на nestjs которая получала бы данные из апи таблиц или crm.

## Допущения в создании схемы

1) users.department можно было вынести в отдельную таблицу. Но аналитика по отделу не требуется. Можно так же вывести все продажи по роли.

## Допущения в нормализации

1) Не полный анализ sql кода в 04_normalize.sql в связи с ограничением по времени 6-8ч.
2) Призрачная сделка 9999 намеренно отброшена. Типа оплаты и прочие данные неизвестны. 
3) Пропущена детальная обработка даты.
4) Не грамотно обработана оплата без сделаки. Возможно лучше ее передавать в отчеты, но с явной настройкой "учитывать оплаты без сделки".
5) Логирование ошибок в нормализации молча записывается в таблицу. Тут еще нужно сделать уведомление на почту/телеграм что бы оперативно правитить расхождения.
